
# Key manager

The key manager is responsible for assigning a name to each mechanism, so that it can be easily accessed by other classes. It also provides a way to retrieve the mechanism associated with a given key in the network tables.

## Key example

Any key should go as follows:

```java
public static final String EXAMPLE_KEY = "Example";
```
